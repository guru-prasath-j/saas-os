# PersonalOS / Amy — PUBLIC demo mode. Sample data only, safe to host.
# OPENAI_API_KEY comes from the host's secret env, not this file.
PERSONALOS_MODE=public
AMY_VAULT=./demo_vault
AMY_PROVIDER_ORDER=openai
AMY_EMBED_BACKEND=st
AMY_EMBED_MODEL=all-MiniLM-L6-v2
